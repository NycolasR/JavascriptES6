"use strict";

var nome = 'Nycolas';
var idade = 19;
var usuario = {
  nome: nome,
  // Se o valor do atributo "nome" for o mesmo da variável "nome", "nome: nome" é o mesmo que "nome,"
  idade: idade,
  empresa: 'IFPB'
};
console.log(usuario);
