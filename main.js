const nome = 'Nycolas'
const idade = 19

const usuario = {
    nome, // Se o valor do atributo "nome" for o mesmo da variável "nome", "nome: nome" é o mesmo que "nome,"
    idade,
    empresa: 'IFPB',
};

console.log(usuario)